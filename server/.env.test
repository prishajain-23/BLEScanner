# Test Environment Configuration
# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blescanner_test
DB_USER=blescanner_test_user
DB_PASSWORD=test_password

# JWT
JWT_SECRET=test_secret_key_for_local_testing_only_not_for_production

# Server
PORT=3001
NODE_ENV=development

# APNs (optional for key testing - using dummy values)
APNS_KEY_ID=dummy
APNS_TEAM_ID=dummy
APNS_TOPIC=com.test.app
APNS_KEY_PATH=./config/dummy.p8
APNS_PRODUCTION=false
